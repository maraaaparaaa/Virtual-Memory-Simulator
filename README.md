# Virtual-Memory-Simulator
Virtual Memory Simulator is an application consisting of a Java backend and a web-based frontend, designed to simulate and visualize virtual memory mechanisms. The project provides an easy-to-use interface and a simulation engine that illustrates concepts such as paging, memory frames, page faults, and page replacement algorithms.
